CREATE SEQUENCE coffee_type_id_seq;

CREATE TABLE coffee_type
(
    id bigint NOT NULL ,
    name character varying,
    price double precision,
    is_enabled boolean,
    CONSTRAINT coffee_type_pkey PRIMARY KEY (id)
);